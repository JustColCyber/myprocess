# Process to enumerate a Linux box

## Operating system and version

cat /etc/os-release

## Path the system looks every time a command is executed 

echo $PATH

## Check out all environment variables that are set for our current user

env
